{# Delete this utility file #}
{% template_specialize_rename 'javascript-app.txt' %}

{# Rename package source files #}
{% set packageFile = package[:1] + package.replace('-', ' ').title().replace(' ', '')[1:] + '.js' -%}
{% template_specialize_rename 'src/package-name/packageName.js', packageFile %}
{% template_specialize_rename 'src/tests/testPackageName.js', 'test' + packageFile[:1].upper() + packageFile[1:] %}

{# Delete the static directory (non-app) #}
{% if noapp is defined and noapp %}
{% template_specialize_rename 'static' %}
{% endif %}

{# Rename package source directory #}
{% template_specialize_rename 'src/package-name', package %}
