{
    "type": "module",
    "name": "{{package}}",
    "version": "0.9.0",
    "description": "{{package}}",
    "keywords": [
        "{{package}}"
    ],
    "homepage": "https://github.com/{{github}}/{{package}}#readme",
    "bugs": "https://github.com/{{github}}/{{package}}/issues",
    "license": "MIT",
    "author": {
        "name": "{{name}}",
        "email": "{{email}}",
        "url": "https://github.com/{{github}}"
    },
    "files": [
        "lib/**/*"
    ],
    "scripts": {
        "test": "ava -v test/"
    }{% if noapp is not defined or not noapp %},
    "dependencies": {
        "element-app": "~1.1",
        "schema-markdown-doc": "~1.2"
    }
{%- endif %}
}
