{
    "type": "module",
    "name": "{{package}}",
    "version": "0.9.0",
    "description": "{{package}}",
    "keywords": [
        "{{package}}"
    ],
    "homepage": "https://github.com/{{github}}/{{package}}#readme",
    "bugs": "https://github.com/{{github}}/{{package}}/issues",
    "license": "MIT",
    "author": {
        "name": "{{name}}",
        "email": "{{email}}",
        "url": "https://github.com/{{github}}"
    },
    "files": [
        "index.js",
        "src/{{package}}/**/*"
    ],
    "scripts": {
        "test": "ava -v 'src/tests/**/*.js'"
    }{% if noapp is not defined or not noapp %},
    "dependencies": {
        "element-model": "~1.0",
        "markdown-model": "~1.1",
        "schema-markdown": "~1.0",
        "schema-markdown-doc": "~1.0"
    }
{%- endif %}
}
